# MMDetection dependencies - Install after base requirements.txt to avoid conflicts
#
# Installation procedure:
# 1. Install base requirements: pip install -r requirements.txt
# 2. Install PyTorch with CUDA 11.8 (should already be installed from Phase 1)
# 3. Uninstall incompatible MMCV 2.2.0 if present:
#    pip uninstall mmcv -y
# 4. Install mmcv with CUDA support (compatible version <2.2.0):
#    pip install mmcv>=2.0.0rc4,<2.2.0 -f https://download.openmmlab.com/mmcv/dist/cu118/torch2.0/index.html
# 5. Install remaining MMDetection dependencies: pip install -r requirements_mmdet.txt
#
# NOTE: mmcv MUST be installed separately with correct CUDA version before other packages
# IMPORTANT: MMDetection requires mmcv<2.2.0 - do NOT use mmcv 2.2.0 or newer

mmengine>=0.8.0
mmdet>=3.0.0
mmcv>=2.0.0rc4,<2.2.0
openmim
matplotlib>=3.5.0
pycocotools>=2.0.0
albumentations>=1.3.0
